# ml-facial-age-detection
This project explores age detection from facial images using a multi-model machine learning approach. It implements K-Means clustering (unsupervised), SVM, and CNNs with Transfer Learning (VGG16). By comparing these paradigms, the study analyzes the complexity of extracting age-specific features and the impact of classification granularity.
